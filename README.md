# JSON Object Arrayifier

Small GitHub Pages utility for converting a whitespace-separated stream of JSON objects:

```json
{"a":1} {"b":2}
```

into a valid JSON array:

```json
[
  {"a":1},
  {"b":2}
]
```
